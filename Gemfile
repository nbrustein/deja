# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Sibling gems under development (published to RubyGems for releases).
gem "llm_mock", path: "../llm_mock"
gem "llm_mock_anthropic", path: "../llm_mock_anthropic"
